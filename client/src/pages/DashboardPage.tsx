/* ── DashboardPage – drag & drop dashboard with multi-dashboard support ── */

import { WidgetGrid } from '@/components/layout/WidgetGrid';

export function DashboardPage() {
  return <WidgetGrid />;
}
